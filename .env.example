# ── 台南公車通知 Bot：本地環境變數範本 ──
# 用法：複製本檔為 .env（cp .env.example .env），填入實際值。
# .env 已被 .gitignore 忽略，不會進版控，機密不外洩。
# 雲端（Cloud Run）不使用本檔，改由 GCP Secret Manager 注入同名變數。

# ── TDX 運輸資料流通服務（公車到站 API）──
# 取得處：https://tdx.transportdata.tw → 註冊會員 → 會員中心 → API 金鑰管理 → 建立金鑰
# 只需這一組即可（1 組 = Client Id + Client Secret），屬「基礎服務」免費額度。
TDX_CLIENT_ID=
TDX_CLIENT_SECRET=

# ── Telegram Bot ──
# 取得處：Telegram 與 @BotFather 對話 → /newbot → 取得 bot token
TELEGRAM_BOT_TOKEN=

# ── 服務自我保護用的隨機密鑰（自行產生，建議 openssl rand -hex 32）──
# TICK_AUTH_TOKEN：Cloud Scheduler 呼叫 /tick 時帶的標頭密鑰
# TELEGRAM_WEBHOOK_SECRET：Telegram 呼叫 /webhook 時帶的 secret_token
TICK_AUTH_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
