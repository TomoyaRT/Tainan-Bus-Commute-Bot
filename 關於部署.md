我發現現在所有的部署相關不管是 GitHub 還是 GCP 還是
  telegram就是這些應該要設定環境變數的東西你都把它變成一個下指令然後把環境變數填充進去的感覺這不是我預期想要的操作我預期的方式是建一個 .env
  檔案然後我把所有相關的環境變數全部寫在裡面然後你透過這樣的方式先去幫我跑那些部署啊設定但是如果是 GCP 的部分我就可以到那 secret management
  那邊去做設定除此之外我希望都放在 .env 檔案裡面就是我的預期是本地的所有環境面數包括那個政府那些 API 的那些 key全部都放在 .env
  檔案裡面所有本地的都歸 .env 檔案都放在裡面然後你需要我去準備什麼我就去準備然後我就放在 .env 檔案但所有雲端相關的就盡量放在那個 secret
  management 裡面但我不確定 github 在去 CICD 觸發 GCP 的步數的時候是不是需要來設定一些東西所以我目前的理解就是本地都放
  .env那其他的部分就如果可以放雲端平台的一些設定環境變數的地方就放進去反正就是做到最基本的一個環境變數的保護而不是暴露在外面

  然後我也不希望讓我用指令的方式去操作
  告訴我去什麼樣的平台做哪些操作取得哪些資料要做什麼樣的設定清清楚楚的告訴我你可以寫成一份文件完整的告訴我我要去哪些平台設定哪些東西建立哪些東西取得哪些資料做哪些設定然後在哪裡做哪些設定在哪裡去建立什麼在哪裡取得什麼樣的資料然後最後這些取得的資料都應該放在點 emv 或者是 gcp 的 secret management就是反正就是分雲端跟本地啦這兩端那就是要有一個地方可以去固定的存放而不是一直用指令的方式讓我去操作我不需要你操作指令

然後大台南公車那些 API你去做最新的深度資料搜索就是去確認我這個專案到底需要哪些 API這些 API 是什麼樣的分類我可以怎麼樣找到這些 API 然後拿回來讓你去使用還是說你知道怎麼使用這些 API 我只需要把那些什麼 key 或者是一些 idea 拿回來給你就可以了因為我想知道的是因為它那個平台很亂很雜所以我想要你去進去幫我看具體來說我到底需要哪些 API然後那些 API 我為什麼需要這樣子
剛才講的那些如果你去最新深度資料搜尋你就是都去查那些公開的資料不要去冒犯到人家網站